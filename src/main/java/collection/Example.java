package collection;

public class Example {
    public static void main(String[] args) {
        // Java 8 added Collection.removeIf(Predicate) that removes all elements
        // matching the predicate, so you can remove all occurrences of null
        // from a list (or any collection)
//        List<String> list = List.
//        list.removeIf(Objects::isNull);
    }
}
